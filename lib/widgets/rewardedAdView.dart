import 'package:flutter/material.dart';

class rewardedAdView extends StatelessWidget {
  const rewardedAdView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
